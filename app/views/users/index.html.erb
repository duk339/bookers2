<p>users/index</p>

<%= render 'users/info', user: current_user %>


<%= render 'books/new', books: @books %>

<% @users.each do |user| %>
  <div>
    <p>投稿ユーザー画像</p>
    <p><%= user.name %></p>
    <P><%= link_to "Show", user_path(user.id) %></P>
  </div>
<% end %>